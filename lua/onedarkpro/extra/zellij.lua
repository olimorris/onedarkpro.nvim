local M = {}

M.template = [[
// Colors - https://github.com/olimorris/onedarkpro.nvim

themes {
    onedarkpro {
        fg "${fg}"
        bg "${bg}"
        black "${black}"
        red "${red}"
        green "${green}"
        yellow "${yellow}"
        blue "${blue}"
        magenta "${purple}"
        cyan "${cyan}"
        white "${white}"
        orange "${orange}"
    }
}
]]

return M
